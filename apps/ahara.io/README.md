# ahara.io

Static landing page served from `apps/ahara.io/static`.

This site targets a single deployment, so defaults are baked in and documented for clarity.

## Architecture

- Static HTML/CSS/JS served as a CDN-backed site.
- No runtime config; edits are made in `static/`.

## Defaults

- Site URL: https://ahara.io
